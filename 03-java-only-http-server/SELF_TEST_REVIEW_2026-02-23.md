# 03 Java Only HTTP Server: 自己テスト採点結果（2026-02-23）

対象ファイル:
- `/Users/ktoyo/Documents/Web-under-the-hood/03-java-only-http-server/SELF_TEST_TEMPLATE.md`

採点方針:
- `2点`: 概ね正確
- `1点`: 一部正しいが曖昧/不正確
- `0点`: 不正確または未回答

---

## 設問別採点（15問 / 30点）

### Q1（1/2）
- 判定: 一部正しい
- あなたの回答の評価:
  - `Javaでインスタンス作成` は正しい。
  - OS側は「クライアントIPとbind」ではなく、「ローカルIP:ポートにbindしてlisten開始」が正確。
- エンジニア表現（推奨）:
  - `ServerSocket生成時に、JVMがOSへsocket/bind/listenを依頼し、8080をLISTEN状態にする。`
- ニュアンス:
  - `bind` は「クライアント情報を結びつける」ではなく「サーバーのローカル終端を予約する」。

### Q2（2/2）
- 判定: 正しい
- エンジニア表現（推奨）:
  - `bindはローカルIP:ポートの割当、listenは接続待受状態への遷移。`
- ニュアンス:
  - `listen` は単なる待機ではなく、接続キューを有効化する意味を含む。

### Q3（0/2）
- 判定: 未回答
- エンジニア表現（推奨）:
  - `ServerSocketは待受専用、accept()で返るSocketは接続ごとの送受信用。`
- ニュアンス:
  - `ServerSocket` と `Socket` は用途が分離された別オブジェクト。

### Q4（2/2）
- 判定: 正しい
- エンジニア表現（推奨）:
  - `curlがURLを解析してHTTP request lineを組み立てる。`
- ニュアンス:
  - OSは送信搬送を担当、HTTP文字列の組立自体はcurl側。

### Q5（1/2）
- 判定: 一部正しい
- あなたの回答の評価:
  - ポート指定への言及は近いが、主目的が不足。
- エンジニア表現（推奨）:
  - `HostヘッダーはHTTP/1.1で必須で、仮想ホスト識別のために使う。`
- ニュアンス:
  - `localhost:8080を読み取る` より `どのホスト向け要求かを明示` が重要。

### Q6（1/2）
- 判定: 一部正しい
- あなたの回答の評価:
  - `BufferedReader` の説明は概ねOK。
  - `requestLine` はURLだけでなく `METHOD PATH VERSION` 全体。
- エンジニア表現（推奨）:
  - `BufferedReaderはSocketのInputStreamを行単位で読むためのラッパーで、ヘッダーを空行まで逐次読み取る。`
  - `requestLineは例: GET /hello HTTP/1.1 の1行を保持し、splitしてmethod/pathを抽出する。`
- ニュアンス:
  - `分析` ではなく `抽出(parse)` と言うと正確。

### Q7（0/2）
- 判定: 未達
- エンジニア表現（推奨）:
  - `HTTPのヘッダー行終端はCRLF（\\r\\n）で、空行（\\r\\n\\r\\n）がヘッダー終了を示す。`
- ニュアンス:
  - `\n` だけでも動く実装はあるが、仕様準拠は `\r\n`。

### Q8（2/2）
- 判定: 正しい
- エンジニア表現（推奨）:
  - `Content-Typeは本文のメディアタイプを示し、クライアントの解釈方法を決める。`
- ニュアンス:
  - `text/plain` はプレーンテキスト、`application/json` はJSON構造データ。

### Q9（0/2）
- 判定: 不正確
- あなたの回答の評価:
  - 404はルーティング結果であり、`Content-Length` 不整合の直接結果ではない。
- エンジニア表現（推奨）:
  - `Content-Length不一致は本文欠落・余剰待機・表示崩れ・接続エラーの原因になる。`
- ニュアンス:
  - ステータスコード異常ではなく、受信側の読み取り境界異常。

### Q10（1/2）
- 判定: 一部正しい
- あなたの回答の評価:
  - `接続終了を伝える` は正しい。
  - 学習メリットが未記述。
- エンジニア表現（推奨）:
  - `Connection: closeで1リクエスト1接続に固定すると、挙動が単純化されデバッグしやすい。`
- ニュアンス:
  - keep-alive管理を避け、学習対象をHTTP基礎に絞る意図。

### Q11（1/2）
- 判定: 一部正しい
- あなたの回答の評価:
  - `TCP接続はできている` は妥当。
  - `レスポンスが返ってこない` より `RSTで強制切断された` が正確。
- エンジニア表現（推奨）:
  - `Connection reset by peerは、相手がTCP RSTで接続を強制終了した状態。`
- ニュアンス:
  - 単なるタイムアウト/無応答とRSTは別事象。

### Q12（1/2）
- 判定: 一部正しい
- あなたの回答の評価:
  - `::1` をIPv6として捉えた点は良い。
  - `63183` は `::1に紐づくポート` ではなく、クライアントの一時ポート。
- エンジニア表現（推奨）:
  - `::1は相手IP、63183は相手のephemeral port。`
- ニュアンス:
  - 表示は `remote socket address`（相手側）である点が重要。

### Q13（2/2）
- 判定: 概ね正しい
- エンジニア表現（推奨）:
  - `TCPは4タプル(clientIP/clientPort/serverIP/serverPort)で接続識別するため双方のポートが必要。`
- ニュアンス:
  - `帰り道` の説明は直感として良い。実務では `接続識別` と言うとより正確。

### Q14（2/2）
- 判定: 正しい
- エンジニア表現（推奨）:
  - `GET + pathの組で分岐し、レスポンスのstatus/content-type/bodyを決定する。`
- ニュアンス:
  - 404は「未定義ルートのフォールバック」。

### Q15（1/2）
- 判定: 一部正しい
- あなたの回答の評価:
  - `curl -vで確認する` は正しい。
  - `status/Content-Type/Content-Length` それぞれの確認観点が不足。
- エンジニア表現（推奨）:
  - `各pathでstatus・Content-Type・Content-Length・body整合を表形式で検証する。`
- ニュアンス:
  - 「指定のない文字列」ではなく「期待値との差分」で検証するのが実務的。

---

## 合計スコア

- `17 / 30 点`
- 正答率目安: `56.7%`

---

## 理解できている領域（強み）

1. ルーティングの全体像（Q14）
2. curlがリクエスト行を作る理解（Q4）
3. `bind/listen` の大枠（Q2）
4. `Content-Type` の基本用途（Q8）
5. クライアント一時ポートの必要性の直感（Q13）

## 曖昧な領域（優先復習）

1. `ServerSocket` と `Socket` の責務分離（Q3）
2. HTTP改行規約 `\r\n`（Q7）
3. `Content-Length` 不整合時の典型症状（Q9）
4. `RST` と無応答/タイムアウトの違い（Q11）
5. `remote address` の読み方（Q12）
6. 検証観点を項目化する力（Q15）

---

## 言葉のニュアンス改善メモ

- `分析する` より `parseする / 抽出する`
- `ポートを読み取る` より `Hostヘッダーで宛先ホストを識別する`
- `帰り道` は直感的には良いが、実務では `4タプルで接続識別` を併記
- `レスポンスが返らない` と `RSTで切断` は区別して表現

---

## 次の学習アクション（短期）

1. Q3/Q7/Q9/Q11/Q12/Q15 だけ再回答（10分）
2. `curl -v` 結果を4エンドポイント表に整理（status/content-type/content-length/body）
3. `JVM_OS_QA.md` の `12-15` セクションを再読して用語を言い換え練習
