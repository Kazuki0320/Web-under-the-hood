# LEARNING_TASKS

このファイルは、今後やることを「優先順」で管理するためのノートです。

## 直近で進める順序

1. Java HTTPサーバーをスレッドプール対応にする
   - 目的: 接続ごとに `new Thread` する方式との差を理解し、同時接続時の安定性と処理性能を改善する
   - 最小ステップ:
     - `ExecutorService`（固定サイズ）で接続処理を実行する
     - 同時リクエスト時に、スレッド数・待機キュー・応答時間の変化を観測する
     - プールサイズを変えて挙動比較し、IO主体ワークロードでの妥当な設定を考える
     - サーバー停止時に `shutdown` / `awaitTermination` で安全終了できるようにする

2. DNSの名前解決フローを確認する
   - 目的: DNS -> HTTP -> HTTPS の入口を固める
   - 最小ステップ:
     - `dig` または `nslookup` で `A` / `AAAA` / `CNAME` を確認する
     - TTLとキャッシュの動きを確認する
     - 1つのドメインを題材に、名前解決の流れを短くメモする

3. Dockerネットワーク観測を行う
   - 目的: Docker特有の通信レイヤー（port mapping/NAT/bridge/サービス名解決）を切り分けて説明できるようにする
   - 最小ステップ:
     - `localhost -> Docker公開ポート -> 前段サーバー -> バックエンド` の経路を観測し、到達順序を確認する
     - `docker inspect` と `docker network inspect` でIP/ネットワーク接続を確認する
     - `ss` / `tcpdump` で待ち受けと実パケットを確認し、設定との差分を特定する
     - 障害パターン（サービス名不一致・network未参加・port不一致）を再現し、失敗ポイントを記録する

4. CORS制約とプリフライトを最小構成で検証する
   - 目的: ブラウザの同一オリジン制約とCORSヘッダー、プリフライトの発生条件を実装ベースで理解する
   - 最小ステップ:
     - FE（`http://localhost:5173`）とBE（`http://localhost:3000`）の別オリジン最小アプリを作る
     - `GET /hello` でシンプルリクエストを通し、`Access-Control-Allow-Origin` の効果を確認する
     - `POST /echo` + `Content-Type: application/json` + `X-Test` でプリフライト（`OPTIONS`）発生を確認する
     - `Access-Control-Allow-Methods` / `Access-Control-Allow-Headers` / Origin不一致の失敗パターンを再現して記録する
     - `credentials: include` と `Access-Control-Allow-Credentials: true` の組み合わせを検証し、`Allow-Origin: *` と併用不可を確認する
     - 最後に「発生条件・必要ヘッダー・失敗時の見分け方」を1枚にまとめる

5. TLS/SSL（HTTPS）とクライアント証明書認証（mTLS）を段階的に検証する
   - 目的: 平文HTTPとの違いを、証明書検証・ハンドシェイク・認証失敗の見え方まで含めて説明できるようにする
   - 最小ステップ:
     - 自己署名CAを作成し、サーバー証明書を発行して `https://localhost` の最小サーバーを起動する
     - `curl -v` で「証明書未信頼時の失敗」と「CAを信頼した時の成功」を比較する
     - 証明書のCN/SAN不一致・期限切れ・中間証明書欠落の失敗パターンを再現して記録する
     - mTLSを有効化し、クライアント証明書なし・不正証明書・正しい証明書で挙動を比較する
     - 最後に「誰を認証しているか（サーバー認証/クライアント認証）」「どこで失敗するか（TLS層/HTTP層）」を1枚にまとめる

## バックエンド学習ロードマップ（50項目）

このセクションは、バックエンドエンジニアとして必要な技術群を順序付きで管理するためのチェックリスト。

### 基礎（1〜10）

1. インターネットの仕組みを理解する（DNS -> HTTP -> HTTPS）
   - DNSの名前解決フローを確認する
   - HTTPS/TLSと証明書検証の要点を確認する
   - `curl -v` でHTTPS通信の確認ログを1本残す
2. サーバーがリクエストとレスポンスをどう処理するか学ぶ ✅
3. クライアント・サーバーアーキテクチャを深く理解する ✅
4. APIの仕組みを学ぶ（RESTの基礎） ✅
5. JSONなどのデータ交換フォーマットを理解する ✅
6. HTTPメソッド・ステータスコード・ヘッダーを学ぶ ✅
7. ステートレスとステートフルの違いを理解する
8. Cookieとセッションの仕組みを学ぶ
9. CORSやセキュリティの基礎を理解する
10. シンプルなバックエンドサーバーを初めて作る ✅

### アプリケーション設計・実装（11〜20）

11. バックエンド言語を1つ極める（Node.js / Python / Java / Go）
12. バックエンドプロジェクトを綺麗に構造化する方法を学ぶ
13. MVCやレイヤードアーキテクチャを理解する
14. ルーティングとミドルウェアの概念を学ぶ
15. アプリケーションで適切にエラーハンドリングする
16. ユーザー入力のバリデーションとサニタイズを行う
17. 環境変数の扱い方を理解する
18. ロギングとデバッグの手法を学ぶ
19. 再利用可能なサービスやユーティリティを作る
20. クリーンで保守しやすいバックエンドコードを書く

### データベース（21〜30）

21. データベースの基礎を習得する
22. SQLを深く学ぶ（PostgreSQL / MySQL）
23. NoSQLデータベースを理解する（MongoDB / Redis）
24. 効率的なデータベーススキーマを設計する
25. インデックスとクエリ最適化を学ぶ
26. マイグレーションを扱う
27. リレーション（1対1 / 1対多 / 多対多）を実装する
28. ORM / ODMを効果的に使う
29. キャッシュ戦略を実装する（Redisなど）
30. データベースパフォーマンスを最適化する

### セキュリティ（31〜40）

31. 安全な認証システムを構築する（JWT / セッション / OAuth）
32. ロールベースの認可を実装する
33. パスワードを安全にハッシュ化する（bcrypt / argon2）
34. 一般的な攻撃からAPIを守る（XSS / CSRF / SQLインジェクション）
35. レートリミットやスロットリングを実装する
36. 環境変数やシークレットを安全に管理する
37. HTTPSと暗号化を利用する
38. API入力の厳格なバリデーションを行う
39. 不審な挙動をログ・監視する
40. バックエンドのセキュリティベストプラクティスに従う

### インフラ・スケーラビリティ（41〜50）

41. Dockerを使ったバックエンドデプロイを学ぶ ✅
42. CI/CDパイプラインを理解する
43. メッセージキューを使う（Kafka / RabbitMQ / SQS）
44. バックグラウンドジョブやワーカーを実装する
45. マイクロサービスアーキテクチャの基礎を学ぶ
46. システムを監視する（ログ / メトリクス / トレーシング）
47. パフォーマンスとスケーラビリティを最適化する
48. クラウド（AWS / GCP / Azure）にデプロイする
49. 実践的なバックエンドプロジェクトを作る（API / SaaS / ダッシュボード）
50. システム思考（スケーラビリティ・信頼性）で考える

### 自作系テーマ（1〜50へ統合）

- 1: 自作CPU（エミュレータ）、自作OS、自作メモリ管理（仮想メモリ）、自作ファイルシステム、自作コンパイラ / インタプリタ
- 2: 自作TCP/IPスタック、自作ソケット通信ライブラリ
- 4: 自作DNSクライアント
- 6: 自作HTTPプロトコル
- 10: 自作HTTPサーバー（ServerSocket）
- 12: 自作アプリケーションサーバー（Tomcat的な）を最小構成で分解
- 13: ミニMVC（Dispatcher -> Router -> Controller -> Service -> Repository）
- 14: 自作ルーティング（Controllerマッピング）
- 19: JSONシリアライザ（Java Object -> JSON）、自作テンプレートエンジン
- 20: 自作バリデーションフレームワーク
- 25: 自作検索エンジン（簡易版）
- 29: 自作キャッシュ（LRUなど）、自作KVS（Redis的な）
- 30: 自作DB（最小実装）、自作トランザクション管理
- 28: 自作ORM（JPA的な）
- 43: 自作メッセージキュー（Kafka的な）
- 44: 自作スレッドプール、自作コネクションプール
- 45: 自作サービスディスカバリ、自作イベントバス
- 47: 自作ロードバランサ
- 48: 自作Webサーバー（Nginx的な）
- 49: 自作ブラウザ（HTMLパーサ + レンダリング）、自作UIフレームワーク
- 50: 自作分散ロック、自作API Gateway、自作DIコンテナ（Springの核）
- 参考学習スタイル: Build your own X 系プロジェクトを題材に、段階的に実装して理解を深める

## 実行順（このリポジトリ向け）

- フェーズ1（短期で一気に）: 1〜10
  - 目安: 2週間〜1ヶ月
  - ゴール: HTTPの全体像を説明し、最小バックエンドを自力で動かせる
- フェーズ2（メイン深掘り）: 11〜20
  - ゴール: 設計・実装・運用しやすさの土台を固める
- フェーズ3（作りながら吸収）: 21〜50
  - ゴール: 実装課題に合わせてDB/セキュリティ/インフラを必要分ずつ習得する

進め方ルール:
- 「順番に完全制覇」ではなく「実装で必要になったタイミングで取得」を優先する
- 各フェーズで必ず1つ成果物を残す（メモ、検証ログ、最小実装）

## 後回しタスク（いったん最下段へ移動）

1. Servlet Filter を触る
   - 目的: Servlet本体の前後に共通処理を差し込む責務分離を理解する
   - 最小ステップ:
     - 全リクエストのURLと処理時間をログ出力するFilterを作る
     - `/admin` の簡易認可チェック（未認可なら `401`）を作る
     - `/*` と `/api/*` で適用範囲を変えて挙動を比較する

2. JVM観測を深掘りする
   - 目的: Javaアプリの遅延・詰まり・異常時に、JVMとOSの境界で原因を切り分けられるようにする
   - 最小ステップ:
     - `jps -l` で対象プロセスを特定し、`jstack <pid>` と `jstack -l <pid>` を読めるようにする
     - `Main.main -> ServerSocket.accept` の待機状態を説明できるようにする
     - `jcmd <pid> VM.version` / `jcmd <pid> VM.system_properties` で実行環境を確認する
     - `lsof -nP -iTCP:8080 -sTCP:LISTEN` と突き合わせて、JVM側とOS側の見え方を対応づける
     - `jcmd <pid> GC.heap_info` などの基本メトリクスを取り、メモリ観測の入口を作る
