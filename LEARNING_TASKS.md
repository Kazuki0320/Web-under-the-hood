# LEARNING_TASKS

このファイルは、今後やることを「優先順」で管理するためのノートです。

## 直近で進める順序

1. フレームワークなしAPIサーバーにDB接続を追加する
   - 目的: Controller / Service / Repository の責務分離を保ちながら、APIからDB永続化までの最小構成を実装できるようにする
   - 最小ステップ:
     - `POST /users` と `GET /users/:id` の最小APIを追加する
     - `Controller -> Service -> Repository` の呼び出し経路を作る
     - RepositoryでDB接続を行い、`INSERT` / `SELECT` を実装する
     - 接続失敗時は `500`、未取得時は `404` を返す
     - `curl` で登録->取得の一連の動作を確認し、実行ログを残す

2. 自作認証サーバー（最小構成）を作る
   - 目的: 認証の基本フロー（ログイン・トークン発行・検証）を実装ベースで理解する
   - 最小ステップ:
     - `POST /login` でID/PWを受け取り、最小トークンを発行する
     - `GET /me` でトークン検証を行い、未認証時は `401` を返す
     - トークン期限切れと不正トークンの失敗パターンを確認する
     - 認証あり/なしのアクセスログ差分を記録する

3. TLS/SSL（HTTPS）とクライアント証明書認証（mTLS）を段階的に検証する
   - 目的: 平文HTTPとの違いを、証明書検証・ハンドシェイク・認証失敗の見え方まで含めて説明できるようにする
   - 最小ステップ:
     - 自己署名CAを作成し、サーバー証明書を発行して `https://localhost` の最小サーバーを起動する
     - `curl -v` で「証明書未信頼時の失敗」と「CAを信頼した時の成功」を比較する
     - 証明書のCN/SAN不一致・期限切れ・中間証明書欠落の失敗パターンを再現して記録する
     - mTLSを有効化し、クライアント証明書なし・不正証明書・正しい証明書で挙動を比較する
     - 最後に「誰を認証しているか（サーバー認証/クライアント認証）」「どこで失敗するか（TLS層/HTTP層）」を1枚にまとめる

4. CORS制約とプリフライトを最小構成で検証する
   - 目的: ブラウザの同一オリジン制約とCORSヘッダー、プリフライトの発生条件を実装ベースで理解する
   - 最小ステップ:
     - FE（`http://localhost:5173`）とBE（`http://localhost:3000`）の別オリジン最小アプリを作る
     - `GET /hello` でシンプルリクエストを通し、`Access-Control-Allow-Origin` の効果を確認する
     - `POST /echo` + `Content-Type: application/json` + `X-Test` でプリフライト（`OPTIONS`）発生を確認する
     - `Access-Control-Allow-Methods` / `Access-Control-Allow-Headers` / Origin不一致の失敗パターンを再現して記録する
     - `credentials: include` と `Access-Control-Allow-Credentials: true` の組み合わせを検証し、`Allow-Origin: *` と併用不可を確認する
     - 最後に「発生条件・必要ヘッダー・失敗時の見分け方」を1枚にまとめる

## バックエンド学習ロードマップ（36項目）

このセクションは、バックエンドエンジニアとして必要な技術群を順序付きで管理するためのチェックリスト。

ステータス凡例: `未対応` / `🟡 着手中` / `✅ 済み`
進捗サマリ（現時点）: 未対応 `24` / 🟡 着手中 `4` / ✅ 済み `8`

### 基礎（1〜10）

| No | 項目 | 状態 |
|---|---|---|
| 1 | インターネットの仕組みを理解する（DNS -> HTTP -> HTTPS） | ✅ 済み |
| 2 | サーバーがリクエストとレスポンスをどう処理するか学ぶ | ✅ 済み |
| 3 | クライアント・サーバーアーキテクチャを深く理解する | ✅ 済み |
| 4 | APIの仕組みを学ぶ（RESTの基礎） | ✅ 済み |
| 5 | HTTPメソッド・ステータスコード・ヘッダーを学ぶ | ✅ 済み |
| 6 | シンプルなバックエンドサーバーを初めて作る | ✅ 済み |
| 7 | JSONなどのデータ交換フォーマットを理解する | 未対応 |
| 8 | ステートレスとステートフルの違いを理解する | 未対応 |
| 9 | Cookieとセッションの仕組みを学ぶ | 未対応 |
| 10 | CORSやセキュリティの基礎を理解する | 未対応 |

### アプリケーション設計・実装（11〜15）

| No | 項目 | 状態 |
|---|---|---|
| 11 | バックエンド言語を1つ極める（Node.js / Python / Java / Go） | 🟡 着手中 |
| 12 | MVCやレイヤードアーキテクチャを理解する | 🟡 着手中 |
| 13 | ルーティングとミドルウェアの概念を学ぶ | 🟡 着手中 |
| 14 | ユーザー入力のバリデーションとサニタイズを行う | 未対応 |
| 15 | ロギングとデバッグの手法を学ぶ | 未対応 |

### データベース（16〜23）

| No | 項目 | 状態 |
|---|---|---|
| 16 | SQLを深く学ぶ（PostgreSQL / MySQL） | 未対応 |
| 17 | NoSQLデータベースを理解する（MongoDB / Redis） | 未対応 |
| 18 | 効率的なデータベーススキーマを設計する | 未対応 |
| 19 | インデックスとクエリ最適化を学ぶ | 未対応 |
| 20 | マイグレーションを扱う | 未対応 |
| 21 | リレーション（1対1 / 1対多 / 多対多）を実装する | 未対応 |
| 22 | ORM / ODMを効果的に使う | 未対応 |
| 23 | キャッシュ戦略を実装する（Redisなど） | 未対応 |

### セキュリティ（24〜31）

この分野の到達目標:
- Auth0 / OpenID Connect / JWT / セッション管理 / Cookie / 自作認証サーバーを説明できる状態にする

| No | 項目 | 状態 |
|---|---|---|
| 24 | 安全な認証システムを構築する（JWT / セッション / OAuth） | 未対応 |
| 25 | ロールベースの認可を実装する | 未対応 |
| 26 | OIDC（OpenID Connect）の認証フローを理解する（ID Token / Access Token / UserInfo） | 未対応 |
| 27 | Auth0で認証基盤を構築する（ログイン・コールバック・トークン検証） | 未対応 |
| 28 | パスワードを安全にハッシュ化する（bcrypt / argon2） | 未対応 |
| 29 | 一般的な攻撃からAPIを守る（XSS / CSRF / SQLインジェクション） | 未対応 |
| 30 | レートリミットやスロットリングを実装する | 未対応 |
| 31 | HTTPSと暗号化を利用する | 未対応 |

### インフラ・スケーラビリティ（32〜36）

| No | 項目 | 状態 |
|---|---|---|
| 32 | Dockerを使ったバックエンドデプロイを学ぶ | ✅ 済み |
| 33 | CI/CDパイプラインを理解する | 未対応 |
| 34 | メッセージキューを使う（Kafka / RabbitMQ / SQS） | 未対応 |
| 35 | バックグラウンドジョブやワーカーを実装する | 未対応 |
| 36 | マイクロサービスアーキテクチャの基礎を学ぶ | ✅ 済み |


### 自作系テーマ（1〜50へ統合）

- 1: 自作CPU（エミュレータ）、自作OS、自作メモリ管理（仮想メモリ）、自作ファイルシステム、自作コンパイラ / インタプリタ
- 2: 自作TCP/IPスタック、自作ソケット通信ライブラリ
- ✅ 3: 自作DNSサーバー（最小実装）
- ✅ 4: 自作HTTPプロトコル
- ✅ 5: 自作HTTPサーバー（ServerSocket）
- ✅ 6: 自作アプリケーションサーバー（Tomcat的な）を最小構成で分解
- 7: ミニMVC（Dispatcher -> Router -> Controller -> Service -> Repository）
- ✅ 8: 自作ルーティング（Controllerマッピング）
- 9: JSONシリアライザ（Java Object -> JSON）、自作テンプレートエンジン
- 10: 自作DB（最小実装）
- 11: 自作ORM（JPA的な）
- 12: 自作スレッドプール、自作コネクションプール
- 13: 自作Webサーバー（Nginx的な）
- 14: 自作ブラウザ（HTMLパーサ + レンダリング）
- 15: 自作DIコンテナ（Springの核）


## 後回しタスク（いったん最下段へ移動）

1. Servlet Filter を触る
   - 目的: Servlet本体の前後に共通処理を差し込む責務分離を理解する
   - 最小ステップ:
     - 全リクエストのURLと処理時間をログ出力するFilterを作る
     - `/admin` の簡易認可チェック（未認可なら `401`）を作る
     - `/*` と `/api/*` で適用範囲を変えて挙動を比較する

2. JVM観測を深掘りする
   - 目的: Javaアプリの遅延・詰まり・異常時に、JVMとOSの境界で原因を切り分けられるようにする
   - 最小ステップ:
     - `jps -l` で対象プロセスを特定し、`jstack <pid>` と `jstack -l <pid>` を読めるようにする
     - `Main.main -> ServerSocket.accept` の待機状態を説明できるようにする
     - `jcmd <pid> VM.version` / `jcmd <pid> VM.system_properties` で実行環境を確認する
     - `lsof -nP -iTCP:8080 -sTCP:LISTEN` と突き合わせて、JVM側とOS側の見え方を対応づける
     - `jcmd <pid> GC.heap_info` などの基本メトリクスを取り、メモリ観測の入口を作る

3. Dockerネットワーク観測を行う
   - 目的: Docker特有の通信レイヤー（port mapping/NAT/bridge/サービス名解決）を切り分けて説明できるようにする
   - 最小ステップ:
     - `localhost -> Docker公開ポート -> 前段サーバー -> バックエンド` の経路を観測し、到達順序を確認する
     - `docker inspect` と `docker network inspect` でIP/ネットワーク接続を確認する
     - `ss` / `tcpdump` で待ち受けと実パケットを確認し、設定との差分を特定する
     - 障害パターン（サービス名不一致・network未参加・port不一致）を再現し、失敗ポイントを記録する

4. Java HTTPサーバーをスレッドプール対応にする
   - 目的: 接続ごとに `new Thread` する方式との差を理解し、同時接続時の安定性と処理性能を改善する
   - 最小ステップ:
     - `ExecutorService`（固定サイズ）で接続処理を実行する
     - 同時リクエスト時に、スレッド数・待機キュー・応答時間の変化を観測する
     - プールサイズを変えて挙動比較し、IO主体ワークロードでの妥当な設定を考える
     - サーバー停止時に `shutdown` / `awaitTermination` で安全終了できるようにする

5. ファイルI/OでCSVファイル読み込みを実装する
   - 目的: バックエンド開発で必要になるファイル入力・レコード分解・エラーハンドリングの基礎を実装ベースで理解する
   - 最小ステップ:
     - `users.csv` を読み込み、ヘッダーとデータ行を分離する
     - 1行ずつ `split` して `id,name,email` を取り出す
     - 列数不一致や空行のスキップを実装する
     - 読み込んだ結果をJSON形式で標準出力できるようにする
     - UTF-8前提で読み込み、文字化けしないことを確認する

6. 「なぜmainが最初に実行されるか」を自作ランチャーで検証する
   - 目的: Java実行時のエントリーポイント解決（JVMが `main` を探して呼ぶ仕組み）を実装ベースで理解する
   - 最小ステップ:
     - `java -cp ... Main` 実行時にJVMが `public static void main(String[] args)` を探す流れを整理する
     - `Class.forName` と `getMethod(\"main\", String[].class)` を使って、`main` を手動呼び出しする小さなランチャーを作る
     - `main` が無いクラス・シグネチャ不一致クラスで失敗パターンを確認する
     - `javap` でクラスの `main` 定義を確認し、ランチャー側の判定条件と対応づける
     - 最後に「通常起動（javaコマンド）と自作ランチャー起動」の違いを短くまとめる

7. Todoアプリ（CRUD）を最小構成で作る
   - 目的: 一般的な業務アプリの基本操作（作成・参照・更新・削除）を一通り実装して、API設計と状態管理の基礎を固める
   - 最小ステップ:
     - `POST /todos`（作成）を実装する
     - `GET /todos` / `GET /todos/:id`（参照）を実装する
     - `PUT /todos/:id`（更新）を実装する
     - `DELETE /todos/:id`（削除）を実装する
     - `404` / `400` のエラーパターンを確認し、`curl` で検証ログを残す
