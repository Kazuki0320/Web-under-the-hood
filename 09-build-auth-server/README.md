# 09 Build Auth Server

自作認証サーバーを段階的に実装して、認証・認可の基本を理解するためのディレクトリ。

## 目的
- フレームワークなしで最小の認証サーバーを作る
- ログイン、トークン検証、失敗時の挙動を確認する

## 最小ゴール
- `POST /login` でID/PWを受け取り、最小トークンを発行できる
- `GET /me` でトークンを検証し、認証済みならユーザー情報を返せる
- 未認証・不正トークン時に `401 Unauthorized` を返せる
- `curl` で正常系と失敗系を再現して挙動を確認できる
