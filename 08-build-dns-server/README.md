# 08-build-dns-server

## 概要
`08` では、最小構成の自作DNSサーバーを作る。

## 目的
- DNSクエリ（まずは `A`）を受け取って応答する流れを実装で理解する
- `dig` で自作サーバーへ直接問い合わせて動作確認できるようにする

## 最小ゴール
- UDPでクエリを受信する
- `A` クエリに固定IPで応答する
- `dig @127.0.0.1 -p 5353 <name> A` で応答を確認する
